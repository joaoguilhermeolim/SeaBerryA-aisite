# =============================================================
#  Netlify configuration — seaberryacai.com
# =============================================================

[functions]
  directory    = "netlify/functions"
  node_bundler = "esbuild"

# -------------------------------------------------------------
#  Language-based automatic routing
#  English browsers → /en/   |   all others → / (PT, default)
# -------------------------------------------------------------
[[redirects]]
  from       = "/*"
  to         = "/en/:splat"
  status     = 302
  force      = false
  conditions = { Language = ["en"] }
